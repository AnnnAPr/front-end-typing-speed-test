// import React from "react";
// import PropTypes from "prop-types";


// const Speed = ({seconds, symbols, setWPM}) => {
//   const wpm = (symbols / 5) / (seconds / 60)
//   setWPM(Math.round(wpm))

//   if (seconds !== 0 && symbols !== 0) {
//     return (
//     <div>
//       {Math.round(wpm)} word/min
//     </div>
//     )
//   }
//   return null
// }

// Speed.propTypes = {
//   seconds: PropTypes.number.isRequired,
//   symbols: PropTypes.func.isRequired,
//   setWPM: PropTypes.arrayOf(PropTypes.object.isRequired).isRequired,
  
// };

// export default Speed